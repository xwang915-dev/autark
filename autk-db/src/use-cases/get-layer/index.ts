/**
 * Export utilities for retrieving a layer as a GeoJSON FeatureCollection.
 *
 * This module provides the use case that converts DuckDB layer tables into
 * GeoJSON, with special handling for raster and building layer types.
 *
 * @module get-layer
 */
export * from './use-case';
