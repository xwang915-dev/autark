/**
 * Use case for dropping a table from the active workspace.
 *
 * Provides a safe operation that returns success/failure information instead of throwing on error.
 *
 * @module drop-table
 */
export { DropTableUseCase } from './use-case';
export type { DropTableParams } from './interfaces';
